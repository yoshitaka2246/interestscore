# interest_score environment variables
# コピーして .env として使用する（.envはGit管理対象外）

# 実行デバイス: auto | cuda | cpu
DEVICE=auto

# Web API (Phase 3以降)
API_HOST=127.0.0.1
API_PORT=8000

# 結果・データの保存先（相対パスの場合はプロジェクトルート基準）
RESULTS_DIR=results
DATA_DIR=data
